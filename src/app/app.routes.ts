/**
 * @module AppRoutes
 * This module defines the client-side navigation paths for the application.
 */

import { Routes } from '@angular/router';

/**
 * An array of objects that represent the application's navigation paths.
 * * @remarks
 * Each route maps a URL path to a specific component.
 * Since this application uses the {@link AppRoutingModule} for its configuration,
 * this array acts as the central source for route definitions.
 * * @example
 * // To add a new route:
 * { path: 'movies', component: MovieCardComponent }
 */
export const routes: Routes = [];
